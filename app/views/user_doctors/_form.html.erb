<%= form_for(@user_doctor) do |f| %>
  <% if @user_doctor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_doctor.errors.count, "error") %> prohibited this user_doctor from being saved:</h2>

      <ul>
      <% @user_doctor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
